<%
  klass = {
    alert: "danger",
    notice: "primary"
  }
%>

<% flash.each do |type, msg| %>
  <div class="alert alert-<%= klass[type.to_sym] %> show position-absolute w-full top-0 end-0 m-5 w-full" role="alert">
    <%= raw msg %>
  </div>
<% end %>
